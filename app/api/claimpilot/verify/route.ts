import { NextResponse } from 'next/server';
import { getSupabaseAdmin } from '@/lib/claimpilot/supabase';

export async function POST(request:Request){const supabase=getSupabaseAdmin();if(!supabase)return NextResponse.json({ok:false,error:'Supabase is not configured'},{status:503});const {settlementId}=await request.json().catch(()=>({}));if(!settlementId)return NextResponse.json({ok:false,error:'settlementId is required'},{status:400});const {data:settlement,error}=await supabase.from('claimpilot_settlements').select('*').eq('id',settlementId).single();if(error||!settlement)return NextResponse.json({ok:false,error:error?.message??'Settlement not found'},{status:404});const verified=Boolean(settlement.official_url&&settlement.claim_url&&settlement.administrator&&settlement.last_verified_at);await supabase.from('claimpilot_settlements').update({status:verified?'open':'verifying',updated_at:new Date().toISOString()}).eq('id',settlementId);return NextResponse.json({ok:true,verified,status:verified?'open':'verifying',checks:{officialAdministrator:Boolean(settlement.administrator),officialSettlementUrl:Boolean(settlement.official_url),officialClaimUrl:Boolean(settlement.claim_url),lastVerified:settlement.last_verified_at??null}});}
